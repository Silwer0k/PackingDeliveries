package ru.hofftech.packingdeliveries.model.dto;

/**
 * Базовый интерфейс для всех основных объектов модели (посылка, грузовик, размещенные посылки)
 * передачи данных (DTO).
 * <p>
 * Используется для типизации и объединения всех DTO классов в проекте,
 * например, для использования в обобщенных (generic) сервисах или контроллерах.
 */
public interface BaseDto {}
